repositories:
  - name: ingress-nginx
    url: https://kubernetes.github.io/ingress-nginx

releases:
  - name: nginx-ingress
    namespace: ingress-nginx
    installed: true
    chart: ingress-nginx/ingress-nginx
    version: "4.11.2"  # Use the latest stable version
